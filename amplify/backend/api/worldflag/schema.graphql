type Point @model @aws_cognito_user_pools @searchable @auth(rules: [{allow: owner}, {allow: public, provider: apiKey, operations: [read, delete]}]) {
  id: ID!
  score: Int! 
  owner: String!
  typedate: String! @index(name: "sortByDate", queryField: "sortByDate", sortKeyFields: ["updatedAt"])
  typescore: String! @index(name: "sortByScore", queryField: "sortByScore", sortKeyFields: ["score"])
  updatedAt: String!
}
 